
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Student {
    std_id        Int      @id @default(autoincrement())
    firstName     String
    middleName    String?
    lastName      String
    date_of_birth DateTime 
    roll_no       Int
    cgpa          Float
    class         Int 
    missed        Int      @default(0)
    fees          Fees[]
    marks         Marks[]
    libraryBooks  LibraryStd[]
    attendance    Attendance[] 
}

model Teacher {
    emp_id        Int     @id @default(autoincrement())
    firstName     String
    middleName    String?
    lastName      String
    date_of_birth DateTime
    dept          String
    paid_leaves   Int     @default(20)
    subject       Subject[]
    marks         Marks[]
    libraryBooks  LibraryTeach[]
}

model Fees {
    fees_reference_id   Int   @id @default(autoincrement())
    std_id              Int
    amount              Int
    paid_on             DateTime @default(now())
    installment_no      Int
    Student_id          Student @relation(fields: [std_id], references: [std_id])
}

model Subject {
    sub_id        Int       @id @default(autoincrement())
    sub_name      String
    emp_id        Int
    class         Int
    Teacher_id    Teacher   @relation(fields: [emp_id], references: [emp_id])
    attendance    Attendance[]
}

model Attendance {
    id            Int       @id @default(autoincrement())
    sub_id        Int
    std_id        Int
    date          DateTime
    present       Boolean
    Subject_id    Subject   @relation(fields: [sub_id], references: [sub_id])
    Student_id    Student   @relation(fields: [std_id], references: [std_id])
}

model Marks {
    id            Int       @id
    std_id        Int 
    emp_id        Int
    subject       String
    Teacher_id    Teacher   @relation(fields: [emp_id], references: [emp_id])
    Student_id    Student   @relation(fields: [std_id], references: [std_id])
}

model LibraryStd {
    id            Int       @id
    bookName      String    
    bookNo        Int
    std_id        Int
    takenOn       DateTime  @default(now())
    dueDate       DateTime
    Student_id    Student   @relation(fields: [std_id], references: [std_id])
}

model LibraryTeach {
    id            Int       @id
    bookName      String    
    bookNo        Int
    emp_id        Int
    takenOn       DateTime  @default(now())
    dueDate       DateTime
    Teacher_id    Teacher   @relation(fields: [emp_id], references: [emp_id])
}


